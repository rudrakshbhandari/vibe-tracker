export VIBE_TRACKER_SHARED_ENV="${VIBE_TRACKER_SHARED_ENV:-$HOME/.config/vibe-tracker/.env.shared}"

if has nvm; then
  use_nvm
fi

dotenv_if_exists "$VIBE_TRACKER_SHARED_ENV"
dotenv_if_exists .env
PATH_add scripts
